<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">

    <!-- Disable cache on story book side.  -->
    <meta http-equiv="Cache-Control" content="no-cache, no-store, must-revalidate"/>
    <meta http-equiv="Pragma" content="no-cache"/>
    <meta http-equiv="Expires" content="0"/>
    <!-- End Disable cache  -->

    <title>{% block title %}{% endblock %}</title>
    {% set basePath = pathsToDependencies.isDockerLocalHost ? pathsToDependencies.localhostDockerContainerPath : pathsToDependencies.repositoryPath %}
    {% include './partial/_drupal-files.twig' %}
    {% include './partial/_common-storybook-js.twig' %}
    <!-- start links to component specific CSS & JS -->
    {% block head %}{% endblock %}
    <!-- end links to component specific CSS & JS -->
</head>
<body data-spy="scroll"
      data-target="#{{ verticalAnchorNavigation.specialCssId.jsNonScrollableContainer }}"
      data-offset="{{ verticalAnchorNavigation.specialBootstrapAttr.htmlBodyOffset }}">
{% block header %}{% endblock %}
<div class="page-container" id="page-container">
    <main id="main-content">
        <div class="region">
            <!-- Start custom code for components -->
            <div class="container">
                {% block content %}{% endblock %}
            </div>
            <!-- End custom code for components -->
        </div>
    </main>
</div>
</body>
</html>

